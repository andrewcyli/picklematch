# PickleMatch Hard Reset Execution Note

This rebuild is not a polish pass.
It is not a cleanup pass.
It is not a chrome-reduction pass.

It is a fresh user-facing rebuild.

If a current UI structure exists but does not fit the new product, discard it.
If a current component contains useful logic, extract the logic and rebuild the interface around it.

The goal is not to make the current product nicer.
The goal is to make the current product irrelevant as a visible baseline.
